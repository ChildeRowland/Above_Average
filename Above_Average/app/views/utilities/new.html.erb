
<div class="utility-instruction">
	<h3>
		Open your Utility bill
	</h3>
	<p>
		Enter a number in each field, and we'll use the compare your carbon footprint against the other users.
	</p>
</div>

<div class="utility-form">
	<%= form_for([@user, @utility], method: :post) do |f| %>
		<%= f.label "Electricity used:" %><br>
		<%= f.text_field :kwh, placeholder: "kwh" %><br>
		<%= f.label "Gas used:" %><br>
		<%= f.text_field :therms, placeholder: "therms" %><br>
		<%= f.label "Water used:" %><br>
		<%= f.text_field :gallons, placeholder: "gallons" %><br>
		<%= f.submit "Submit" %>
	<% end %>
</div>

<div>
	<% if @utility.errors.full_messages.any? %>
		<ul>
			<% @utility.errors.full_messages.each do |error_message| %>
				<li><%= error_message %></li> <br />
   			<% end %>
   		<ul>
	<% end %>
</div>



