<p>
	Tell us about your household utilities bill.  Enter a number in each field, and we'll use the compare your carbon footprint against the other users.
</p>

<%= form_for([@user, @utility], method: :post) do |f| %>
	<%= f.label "Enter the kWh of electricity used:" %><br>
	<%= f.text_field :kwh %><br>
	<%= f.label "Enter the therms of gas used:" %><br>
	<%= f.text_field :therms %><br>
	<%= f.label "Enter the gallons of water used:" %><br>
	<%= f.text_field :gallons %><br>
	<%= f.submit "Submit" %>
<% end %>

<div>
	<% if @utility.errors.full_messages.any? %>
		<ul>
			<% @utility.errors.full_messages.each do |error_message| %>
				<li><%= error_message %></li> <br />
   			<% end %>
   		<ul>
	<% end %>
</div>



